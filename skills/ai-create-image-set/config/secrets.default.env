# Default placeholder — DO NOT put real keys here.
# ------------------------------------------------------------------
# The user supplies their own OpenAI API key at:
# ~/.claude/.env/ai-create-image-set.env
#
# Resolution order in the wrapper:
# 1. $OPENAI_API_KEY (environment)
# 2. ~/.claude/.env/ai-create-image-set.env (per-user)
# 3. This file (empty — forces the user to supply a key)
# ------------------------------------------------------------------

OPENAI_API_KEY=
